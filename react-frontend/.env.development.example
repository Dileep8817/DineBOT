# Copy to react-frontend/.env.development (gitignored).
# Must match API_KEY in the backend root .env — injected by dev server proxy only (not in JS bundle).

DINEBOT_PROXY_API_KEY=change-me-in-production
# DINEBOT_PROXY_TARGET=http://127.0.0.1:8000
