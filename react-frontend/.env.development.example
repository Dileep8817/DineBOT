# Copy to .env.development (that file is gitignored — do not commit real keys).

DINEBOT_PROXY_API_KEY=dinebot-local-dev
# DINEBOT_PROXY_TARGET=http://127.0.0.1:8000

# Stripe Payment Element (publishable key only; never put secret keys in the frontend)
# REACT_APP_STRIPE_PUBLISHABLE_KEY=pk_test_xxxxxxxx

# Default restaurant when ?restaurant_id= is not in the URL (must match backend menu id)
# REACT_APP_DEFAULT_RESTAURANT_ID=restaurant_1
