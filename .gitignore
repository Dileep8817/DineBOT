# secrets never commit
.env

# Local-only restaurant data (optional overrides)
data/local/

# Track sample restaurant JSON; add other venues under data/ as needed (gitignore those dirs if private)

# chroma vector DB
chroma_data/

.DS_Store